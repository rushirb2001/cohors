# Rust
/target
**/target
Cargo.lock        # keep for the binary crate; remove this line if you decide to commit it
*.rs.bk
*.pdb

# WASM / web build output
/crates/cohors-web/dist
/crates/cohors-web/pkg

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# cohors runtime artifacts (in case they ever land in-tree during dev)
/.cohors-cache
*.log

# Demo recording scratch
*.gif.tmp
